{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "attach",
      "name": "Attach to Node",
      "restart": true,
      "localRoot": "${workspaceFolder}",
      "sourceMapPathOverrides": {
        "/src/*": "${workspaceRoot}/*"
      },
      "remoteRoot": "/src/"
    }
  ]
}
