{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "attach",
      "name": "Attach to Node",
      "restart": true,
      "port": 8001,
      "address": "localhost",
      "localRoot": "${workspaceFolder}",
      "sourceMapPathOverrides": {
        "/src/*": "${workspaceRoot}/*"
      },
      "remoteRoot": "/src/"
    }
  ]
}
