# Migration `20201109213518`

This migration has been generated by marciorasf at 11/9/2020, 6:35:18 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."UserToken" ADD COLUMN "userId" integer   NOT NULL 

ALTER TABLE "public"."UserToken" ADD FOREIGN KEY("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201109213352..20201109213518
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -15,11 +15,13 @@
   Category   Category[]
 }
 model UserToken {
-  id        Int @id @default(autoincrement())
+  id        Int       @id @default(autoincrement())
   token     Int
   expiresIn DateTime
+  user      User      @relation(fields: [userId], references: [id])
+  userId    Int
 }
 model Flashcard {
   id           Int       @id @default(autoincrement())
```


